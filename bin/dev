#!/usr/bin/env sh

if gem list foreman -i --silent; then
  exec foreman start -f Procfile.dev --env /dev/null "$@"
else
  echo "Installing foreman..."
  gem install foreman && exec foreman start -f Procfile.dev --env /dev/null "$@"
fi
